# UI Redesign

The previous interface is preserved on the `legacy-ui` branch at commit `1359812ae93a3542b9295702a95a78c2baaf8eca`.

The active `main` branch uses the redesigned interface. Functional DOM hooks and editor behavior are intentionally preserved so the 3D, UV, history, autosave, drag and drop, export, and QA flows can continue to use the same application logic.

Rollback target: `legacy-ui`.
